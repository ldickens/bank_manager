def format_bank(data: list[list[str]]):
    formatted = []
    for entry in data:
        pass


# def parse_json(media: MEDIA_MAP | MEDIA_FILE) -> MEDIA_MAP_ENTRY:
#     data = []
#     for entries in media:
#         for entry in entries:
#             entry = []
#             for prop in entry:
#                 entry.append(prop)
#             data.append(entry)
#     return data
